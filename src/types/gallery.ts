export interface Gallery {
  id: string;

  /* =========================================================
     YOUTUBE VIDEO URL
  ========================================================= */

  video: string;

  /* =========================================================
     CAPTION
  ========================================================= */

  caption: string | null;

  /* =========================================================
     DISPLAY ORDER
  ========================================================= */

  sort_order: number;

  /* =========================================================
     TIMESTAMPS
  ========================================================= */

  created_at?: string;

  updated_at?: string;
}